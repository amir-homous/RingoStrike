VITE_BASE=/

# Same-origin production behind Nginx should leave VITE_API_BASE unset.
# The frontend will call /auth/login, /me, /challenges, etc. on the same host.
# VITE_API_BASE=

# If the API is hosted separately, set the full API origin instead:
# VITE_API_BASE=https://api.ringostrike.com
